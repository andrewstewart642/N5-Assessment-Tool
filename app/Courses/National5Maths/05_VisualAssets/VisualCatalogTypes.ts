/**
 * @deprecated Historical visual evidence is now owned by
 * ../CatalogVisualEvidenceTypes. This path remains only for the 2014 pilot
 * entries until their normal catalogue migration removes the old import.
 */
export * from "../CatalogVisualEvidenceTypes";
